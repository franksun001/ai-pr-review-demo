# Disabled for the free CodeRabbit demo path.
# This workflow needs secrets.ANTHROPIC_API_KEY and will fail without it.
# To re-enable: restore the previous job and add the Anthropic secret.
#
# Prefer CodeRabbit GitHub App for $0 public-repo demos.
name: AI PR Review (Anthropic — disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Use CodeRabbit for free demo. Re-enable Claude Action when you have ANTHROPIC_API_KEY."
